# libpng1650-precompiled

this is a list of precompiled headers and libraries for libpng, directly paste into mingw bin, include, lib folders and compile with -lpng -lzlib

precompiled on an Intel I7-9750H processor
